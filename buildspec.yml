version: 0.2

phases:      
  build:
    commands:
      - echo Build started on `date`
      - sh scripts/build.sh
      - go build duoauth/duoauth.go
      - go install
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - duoauth
